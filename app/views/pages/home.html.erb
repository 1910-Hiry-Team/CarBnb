<h1>Pages#home</h1>
<p>Find me in app/views/pages/home.html.erb</p>



<%= form_with(url: search_cars_path, method: :get, local: true) do |f| %>
  <div>
    <label for="car_type">Car Type</label>
    <%= f.select :car_type, ['SUV', 'Sedan', 'Truck', 'Van'], include_blank: 'Any' %>
  </div>
  <div>
    <label for="address">Address</label>
    <%= f.text_field :address, placeholder: 'Enter a location' %>
  </div>
  <div>
    <label for="radius">Radius (miles)</label>
    <%= f.select :radius, [5, 20, 50, 100, 200] %>
  </div>
  <div>
    <label for="price_min">Price Range</label>
    <%= f.number_field :price_min, placeholder: 'e.g. 50' %>
  </div>
  <div>
    <label for="price_max">Price Range</label>
    <%= f.number_field :price_max, placeholder: 'e.g. 100' %>
  </div>
  <%= f.submit 'Search', class: 'btn btn-primary' %>
<% end %>
