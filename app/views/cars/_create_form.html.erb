# simple form to create a new car
<%= simple_form_for [current_user, car] do |f| %>
  <%= f.input :brand %>
  <%= f.input :model %>
  <%= f.input :category %>
  <%= f.input :address %>
  <%= f.input :price_per_hour, as: :integer %>
  <%= f.button :submit %>
<% end %>
